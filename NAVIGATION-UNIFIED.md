# 🎯 Универсальная навигация FodiFood

## Описание

Единая навигационная система, объединяющая:
- ✅ Мобильную навигацию с Sheet (боковая панель)
- ✅ Десктопную навигацию с Dropdown menu
- ✅ Систему авторизации с паролем
- ✅ Режим редактирования контента
- ✅ Корзину и переключатель темы
- ✅ Адаптивный дизайн

## Использование

### Базовое использование

```tsx
import { Navigation } from '@/components/Navigation';

export default function Page() {
  return (
    <div>
      <Navigation />
      {/* Ваш контент */}
    </div>
  );
}
```

### С опциями

```tsx
<Navigation 
  showEditMode={true}  // Показывать функционал редактирования (по умолчанию: true)
/>
```

## Возможности

### 📱 Мобильная версия
- Sheet боковая панель слева
- Список навигационных ссылок с иконками
- Кнопки входа/выхода
- Кнопки редактирования (если авторизован)
- Кнопка сохранения (если есть изменения)

### 💻 Десктопная версия
- Горизонтальная навигация по центру
- Dropdown меню профиля (если авторизован)
- Кнопки редактирования и сохранения
- Телефон контактов
- Корзина и переключатель темы

### 🔐 Авторизация
- Модальное окно ввода пароля
- Проверка через API (`authAPI.login()`)
- Сохранение токена в localStorage
- Автоматическая проверка при загрузке

### ✏️ Режим редактирования
- Кнопка "Редактировать" / "Просмотр"
- Кнопка "Сохранить" (появляется при изменениях)
- Интеграция с `useRestaurant()` контекстом
- Toast уведомления об успехе/ошибке

## Структура навигационных элементов

По умолчанию включает:
- 🏠 Главная (`/`)
- 🍱 Меню (`/menu`)
- 👤 О нас (`#about`)
- 🚚 Доставка (`#delivery`)
- 📞 Контакты (`#contacts`)

Для изменения отредактируйте массив `navItems` в `Navigation.tsx`.

## Зависимости

- `@radix-ui/react-dialog` - для Sheet
- `@radix-ui/react-dropdown-menu` - для Dropdown
- `framer-motion` - для анимаций
- `lucide-react` - для иконок
- `sonner` - для toast уведомлений

## API интеграция

Использует:
- `authAPI.login(password)` - авторизация
- `authAPI.logout()` - выход
- `authAPI.isAuthenticated()` - проверка статуса
- `useRestaurant()` - контекст ресторана для редактирования

## Стилизация

- Прозрачный фон с blur-эффектом
- Изменение при скролле (тень и непрозрачность)
- Градиент для логотипа
- Адаптивные размеры (h-16 на мобильных, h-20 на десктопе)
- Border transition анимации

## Удаление старых файлов

После внедрения универсальной навигации можно удалить:

```bash
# Старая навигация (больше не используется)
rm src/components/Restaurant/RestaurantNavbar.tsx

# Можно удалить отдельные компоненты (теперь все в Navigation.tsx)
# rm src/components/Navigation/Header.tsx
# rm src/components/Navigation/MobileNav.tsx
# rm src/components/Navigation/DesktopNav.tsx
```

## Troubleshooting

### Не работает режим редактирования
Убедитесь, что у вас настроен `RestaurantContext`:
```tsx
// В вашем layout или странице
<RestaurantProvider>
  <Navigation />
  {children}
</RestaurantProvider>
```

### Не работает авторизация
Проверьте, что `authAPI` правильно настроен в `@/lib/api-client`:
```tsx
export const authAPI = {
  login: async (password: string) => { /* ... */ },
  logout: () => { /* ... */ },
  isAuthenticated: () => { /* ... */ }
};
```

### Sheet не открывается
Убедитесь, что установлен `@radix-ui/react-dialog`:
```bash
npm install @radix-ui/react-dialog
```

## Сравнение с предыдущими версиями

| Функция | RestaurantNavbar | Header+MobileNav+DesktopNav | Navigation (новая) |
|---------|------------------|------------------------------|---------------------|
| Мобильное меню | motion.div | Sheet | ✅ Sheet |
| Авторизация | ✅ Есть | ❌ Нет | ✅ Есть |
| Режим редактирования | ✅ Есть | ❌ Нет | ✅ Есть |
| Dropdown профиля | ❌ Нет | ✅ Есть | ✅ Есть |
| Компонентов | 1 файл | 3 файла | 1 файл |
| Доступность (a11y) | Средняя | ✅ Отлично | ✅ Отлично |

## Итог

✅ **Рекомендуется использовать `Navigation`** - единственный компонент со всеми возможностями!
