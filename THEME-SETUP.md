# 🎨 Настройка тем (Светлая/Тёмная)

## ✅ Что было настроено

### 1. Правильная смена фона

**Светлая тема:**
- 🌞 Почти белый фон (oklch 0.99)
- 📝 Тёмный текст для контраста
- 🎨 Белые карточки
- 🟠 Оранжевый primary цвет

**Тёмная тема:**
- 🌙 Тёмный фон (oklch 0.12)
- 📝 Светлый текст
- 🎨 Тёмные карточки с лёгким тоном
- 🟠 Яркий оранжевый primary цвет

### 2. Плавные переходы

```css
body {
  transition: background-color 0.3s ease, color 0.3s ease;
}
```

При переключении темы фон и текст плавно меняются за 0.3 секунды.

### 3. Цветовая схема

#### Светлая тема (`:root`)
```css
--background: oklch(0.99 0 0);        /* Почти белый */
--foreground: oklch(0.15 0.02 264);   /* Тёмный текст */
--card: oklch(1 0 0);                 /* Белые карточки */
--primary: oklch(0.55 0.22 27);       /* Оранжевый */
--border: oklch(0.90 0.01 264);       /* Светлая граница */
```

#### Тёмная тема (`.dark`)
```css
--background: oklch(0.12 0.02 264);   /* Тёмный фон */
--foreground: oklch(0.95 0.01 264);   /* Светлый текст */
--card: oklch(0.18 0.02 264);         /* Тёмные карточки */
--primary: oklch(0.60 0.24 27);       /* Яркий оранжевый */
--border: oklch(0.25 0.02 264);       /* Тёмная граница */
```

### 4. Адаптивный скроллбар

Скроллбар автоматически подстраивается под тему:
- Трек: `bg-muted` (меняется с темой)
- Ползунок: `bg-primary` (оранжевый в обеих темах)

## 🎯 Как это работает

### 1. ThemeProvider (next-themes)

В `providers.tsx`:
```tsx
<ThemeProvider attribute="class" defaultTheme="system" enableSystem>
  {children}
</ThemeProvider>
```

- `attribute="class"` - добавляет класс `.dark` к `<html>`
- `defaultTheme="system"` - следует системной теме
- `enableSystem` - автоматическое определение темы ОС

### 2. Переключатель темы

Компонент `ThemeToggle.tsx`:
```tsx
const { theme, setTheme } = useTheme();

onClick={() => setTheme(theme === "dark" ? "light" : "dark")}
```

### 3. CSS переменные

В `globals.css` определены две схемы:
- `:root` - светлая тема (по умолчанию)
- `.dark` - тёмная тема

При переключении next-themes добавляет/удаляет класс `.dark` на `<html>`.

## 🎨 Основные цвета

### Primary (Оранжевый)
- **Светлая**: `oklch(0.55 0.22 27)` - насыщенный оранжевый
- **Тёмная**: `oklch(0.60 0.24 27)` - более яркий оранжевый

### Background
- **Светлая**: `oklch(0.99 0 0)` - почти белый
- **Тёмная**: `oklch(0.12 0.02 264)` - очень тёмный синеватый

### Foreground (текст)
- **Светлая**: `oklch(0.15 0.02 264)` - почти чёрный
- **Тёмная**: `oklch(0.95 0.01 264)` - почти белый

### Card (карточки)
- **Светлая**: `oklch(1 0 0)` - чисто белый
- **Тёмная**: `oklch(0.18 0.02 264)` - тёмно-серый

## 🔧 Кастомизация

### Изменить primary цвет

В `globals.css`:
```css
:root {
  --primary: oklch(0.55 0.22 27);  /* Ваш цвет для светлой темы */
}

.dark {
  --primary: oklch(0.60 0.24 27);  /* Ваш цвет для тёмной темы */
}
```

### Изменить фон

```css
:root {
  --background: oklch(0.99 0 0);   /* Светлый фон */
}

.dark {
  --background: oklch(0.12 0.02 264);  /* Тёмный фон */
}
```

### Добавить новый цвет

```css
:root {
  --my-color: oklch(0.7 0.15 120);  /* Зелёный для светлой темы */
}

.dark {
  --my-color: oklch(0.8 0.18 120);  /* Светло-зелёный для тёмной темы */
}
```

Использование в Tailwind:
```tsx
<div className="bg-[--my-color]">...</div>
```

## 📱 Автоматическое определение темы

При первом посещении сайт автоматически определяет тему ОС:
- 🌞 macOS Light Mode → светлая тема
- 🌙 macOS Dark Mode → тёмная тема
- 🔄 При изменении темы ОС → автоматическое переключение

## ✨ Фичи

- ✅ Плавное переключение (0.3s transition)
- ✅ Автоматическое определение системной темы
- ✅ Сохранение выбора в localStorage
- ✅ Адаптивный скроллбар
- ✅ Контрастные цвета для доступности
- ✅ OKLCH цветовое пространство (современное)

## 🎯 Использование в компонентах

Все компоненты автоматически поддерживают обе темы благодаря CSS переменным:

```tsx
// Автоматически меняется с темой
<div className="bg-background text-foreground">
<Card className="bg-card">
<Button className="bg-primary text-primary-foreground">
```

## 🔍 Отладка

### Проверить текущую тему
```tsx
const { theme } = useTheme();
console.log(theme); // "light", "dark" или "system"
```

### Принудительно установить тему
```tsx
const { setTheme } = useTheme();
setTheme("dark");  // или "light", "system"
```

## 📖 Дополнительно

- Документация next-themes: https://github.com/pacocoursey/next-themes
- OKLCH цвета: https://oklch.com
- shadcn/ui темы: https://ui.shadcn.com/themes

---

Теперь сайт-резюме полностью поддерживает красивую смену светлой и тёмной темы! 🎨✨
