# Модальное окно с деталями заказа в админ-дашборде

## ✅ Реализовано

### Frontend
Файл: `src/app/admin/dashboard/page.tsx`

#### Добавлена функциональность:

1. **State для модального окна**
   ```typescript
   const [selectedOrder, setSelectedOrder] = useState<any>(null);
   const [orderDetails, setOrderDetails] = useState<any>(null);
   const [isLoadingDetails, setIsLoadingDetails] = useState(false);
   ```

2. **Функция загрузки деталей заказа**
   ```typescript
   const loadOrderDetails = async (orderId: number) => {
     setIsLoadingDetails(true);
     try {
       const details = await ordersAPI.getByIdAdmin(orderId);
       setOrderDetails(details);
     } catch (error) {
       console.error('Failed to load order details:', error);
       toast.error('Ошибка загрузки деталей заказа');
     } finally {
       setIsLoadingDetails(false);
     }
   };
   ```

3. **Кнопка "Детали" для каждого заказа**
   - Добавлена кнопка с иконкой `Eye`
   - При клике вызывается `handleOrderClick(order)`
   - Загружает полные детали заказа через API

4. **Модальное окно с деталями заказа**
   Отображает:
   - **Статус заказа** с цветными бейджами
   - **Информация о клиенте**: имя, телефон, email
   - **Адрес доставки**: улица, дом, квартира, этаж, подъезд, домофон
   - **Состав заказа**: список всех блюд с количеством и ценой
   - **Особые пожелания**: комментарии от клиента
   - **Итого**: подытог, доставка, налог, общая сумма, способ оплаты

### Backend
Файл: `backend/src/handlers/restaurant_orders.rs`

#### Endpoint уже существует:
```rust
// Get order by ID with items - Admin only
pub async fn get_order_by_id(
    State(pool): State<PgPool>,
    Path(id): Path<i32>,
) -> Result<Json<OrderWithItems>, AppError>
```

- **URL**: `GET /api/restaurant/admin/orders/{id}`
- **Auth**: Требуется JWT токен
- **Response**: 
  ```json
  {
    "order": {
      "id": 1,
      "order_number": "ORD-1234-5678",
      "customer_name": "Иван Иванов",
      "customer_phone": "+48123456789",
      "status": "pending",
      "total": "45.50",
      ...
    },
    "items": [
      {
        "id": 1,
        "menu_item_name": "Пицца Маргарита",
        "menu_item_price": "12.50",
        "quantity": 2,
        "special_instructions": "Без лука"
      }
    ]
  }
  ```

## 🎨 UI компоненты

### Кнопка "Детали"
- Позиция: справа от каждого заказа в списке
- Стиль: `outline` variant
- Иконка: `Eye`
- Текст: "Детали"

### Модальное окно
- **Overlay**: полупрозрачный черный фон (`bg-black/50`)
- **Размер**: max-width 2xl, максимальная высота 90vh
- **Скролл**: вертикальный при большом количестве контента
- **Закрытие**: клик по overlay или кнопка X
- **Sticky header**: заголовок остается на месте при скролле

### Секции модального окна
1. **Заголовок**: номер заказа + дата создания
2. **Статус**: цветной бейдж с эмодзи
3. **Клиент**: имя, телефон, email
4. **Адрес**: полный адрес доставки
5. **Блюда**: карточки с названием, количеством, ценой
6. **Итого**: разбивка стоимости + способ оплаты

## 📱 Использование

1. **Войти в админ-панель**: `/admin/dashboard`
2. **Найти заказ** в списке "Последние заказы"
3. **Нажать "Детали"** на нужном заказе
4. **Просмотреть информацию** в модальном окне
5. **Закрыть** кликом по overlay или кнопке X

## 🔐 Безопасность

- Endpoint защищен JWT аутентификацией
- Доступ только для администраторов
- При ошибке 401 показывается toast уведомление
- Автоматическое перенаправление на login при отсутствии токена

## 🎯 Статусы заказов

| Статус | Цвет | Эмодзи | Описание |
|--------|------|--------|----------|
| `pending` | Желтый | 🕐 | Ожидает подтверждения |
| `preparing` | Синий | 👨‍🍳 | Готовится |
| `ready` | Зеленый | ✅ | Готов к выдаче |
| `delivered` | Серый | 🚚 | Доставлен |
| `cancelled` | Красный | ❌ | Отменен |

## 🚀 Тестирование

### Локально
1. Запустить dev сервер: `npm run dev`
2. Открыть: `http://localhost:3001/admin/dashboard`
3. Войти с credentials: `admin` / `admin123`
4. Кликнуть "Детали" на любом заказе

### Production
- URL: https://your-domain.com/admin/dashboard
- Backend API: https://portfolio-a4yb.shuttle.app

## 📝 Будущие улучшения

- [ ] Редактирование статуса заказа прямо из модального окна
- [ ] Кнопка отмены заказа
- [ ] История изменений статуса
- [ ] Печать чека/квитанции
- [ ] Экспорт деталей в PDF
- [ ] Копирование адреса в буфер обмена
- [ ] Звонок клиенту (tel: link)
- [ ] Отправка SMS/Email уведомлений
