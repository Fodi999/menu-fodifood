# Публичный доступ к заказам в админ-дашборде

## ✅ Изменения

### Backend (Rust/Axum)

Файл: `backend/src/main.rs`

**Перемещены endpoints из защищённых в публичные:**

```rust
// БЫЛО (требовали JWT):
.route("/api/restaurant/admin/orders", get(handlers::restaurant_orders::get_all_orders))
.route("/api/restaurant/admin/orders/{id}", get(handlers::restaurant_orders::get_order_by_id))

// СТАЛО (публичный доступ):
let restaurant_public_pool = Router::new()
    // ... other routes
    .route("/api/restaurant/admin/orders", get(handlers::restaurant_orders::get_all_orders))
    .route("/api/restaurant/admin/orders/{id}", get(handlers::restaurant_orders::get_order_by_id))
    .with_state(pool.clone());
```

**Защищёнными остались только endpoints для изменения:**
- `PUT /api/restaurant/admin/orders/{id}/status` - изменение статуса
- `PUT /api/restaurant/admin/orders/{id}/cancel` - отмена заказа

### Frontend (Next.js)

Файл: `src/app/admin/dashboard/page.tsx`

**Удалено:**
1. ❌ Проверка `authAPI.isAuthenticated()` при загрузке страницы
2. ❌ State `ordersLoadError`, `showLoginForm`, `loginPassword`, `loginLoading`
3. ❌ Функция `handleLogin`
4. ❌ Форма входа в модальном окне
5. ❌ Сообщение "Требуется авторизация"

**Результат:**
- ✅ Дашборд открывается БЕЗ авторизации
- ✅ Заказы загружаются сразу
- ✅ Можно просматривать детали заказов
- ✅ WebSocket работает в реальном времени

## 🔐 Безопасность

### Что ДОСТУПНО публично:
- ✅ Просмотр всех заказов (`GET /api/restaurant/admin/orders`)
- ✅ Просмотр деталей заказа (`GET /api/restaurant/admin/orders/{id}`)
- ✅ Просмотр категорий и меню
- ✅ Создание нового заказа (`POST /api/restaurant/orders`)

### Что ЗАЩИЩЕНО (требует JWT):
- 🔒 Изменение статуса заказа (`PUT /orders/{id}/status`)
- 🔒 Отмена заказа (`PUT /orders/{id}/cancel`)
- 🔒 Создание/изменение/удаление категорий
- 🔒 Создание/изменение/удаление блюд меню
- 🔒 Изменение информации о ресторане

## 📊 Архитектура доступа

```
┌──────────────────────────────────────────┐
│  PUBLIC ACCESS (без авторизации)         │
├──────────────────────────────────────────┤
│  • Просмотр меню                         │
│  • Просмотр категорий                    │
│  • Создание заказа                       │
│  • Просмотр своего заказа (по номеру)    │
│  • Просмотр ВСЕХ заказов (админ панель)  │ ← НОВОЕ
│  • Детали заказа с items                 │ ← НОВОЕ
│  • WebSocket аналитика                   │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│  PROTECTED ACCESS (JWT токен)            │
├──────────────────────────────────────────┤
│  • Изменение статуса заказа              │
│  • Отмена заказа                         │
│  • CRUD операции с меню                  │
│  • CRUD операции с категориями           │
│  • Управление рестораном                 │
└──────────────────────────────────────────┘
```

## 🚀 Использование

### Открытие админ-дашборда

```bash
# Локально
http://localhost:3001/admin/dashboard

# Production
https://your-domain.com/admin/dashboard
```

**БЕЗ логина сразу видны:**
- Статистика (всего заказов, доход, категории, блюда)
- Список последних 5 заказов
- Детали каждого заказа при клике "Детали"
- Real-time обновления через WebSocket

### Для изменения заказов

Если нужно **изменить статус** или **отменить заказ** - потребуется авторизация:

1. Вызов `authAPI.login('admin123')`
2. Получение JWT токена
3. Токен автоматически добавляется в заголовки запросов

## 📝 API Endpoints

### Публичные (без токена)

```bash
# Получить все заказы
GET https://portfolio-a4yb.shuttle.app/api/restaurant/admin/orders

# Получить заказ с деталями
GET https://portfolio-a4yb.shuttle.app/api/restaurant/admin/orders/1

# Получить заказ по номеру
GET https://portfolio-a4yb.shuttle.app/api/restaurant/orders/ORD-1234-5678
```

### Защищённые (с JWT токеном)

```bash
# Изменить статус
PUT https://portfolio-a4yb.shuttle.app/api/restaurant/admin/orders/1/status
Headers: Authorization: Bearer <JWT_TOKEN>
Body: {"status": "preparing"}

# Отменить заказ
PUT https://portfolio-a4yb.shuttle.app/api/restaurant/admin/orders/1/cancel
Headers: Authorization: Bearer <JWT_TOKEN>
```

## ⚠️ Важные замечания

1. **Данные видны всем**: Любой может открыть дашборд и увидеть заказы
2. **Конфиденциальность**: Отображаются имена, телефоны, адреса клиентов
3. **Рекомендация**: В production добавить базовую авторизацию или IP whitelist
4. **WebSocket**: Также публичный - любой может подключиться и получать обновления

## 🔧 Будущие улучшения безопасности

```rust
// Опции для production:

// 1. Базовая HTTP авторизация
.layer(tower_http::auth::RequireAuthorizationLayer::basic("admin", "password"))

// 2. IP whitelist
.layer(axum_middleware::from_fn(ip_whitelist_middleware))

// 3. Rate limiting
.layer(tower::limit::RateLimitLayer::new(100, Duration::from_secs(60)))

// 4. CORS только для своего домена
.allow_origin("https://your-domain.com".parse::<HeaderValue>().unwrap())
```

## ✅ Проверка работоспособности

После деплоя проверьте:

```bash
# 1. Заказы загружаются
curl https://portfolio-a4yb.shuttle.app/api/restaurant/admin/orders

# 2. Детали заказа доступны
curl https://portfolio-a4yb.shuttle.app/api/restaurant/admin/orders/1

# 3. Изменение статуса БЕЗ токена - должна быть ошибка 401
curl -X PUT https://portfolio-a4yb.shuttle.app/api/restaurant/admin/orders/1/status \
  -H "Content-Type: application/json" \
  -d '{"status": "preparing"}'
```

Ожидаемый результат для #3: `{"error":"Invalid token"}` или `401 Unauthorized`

## 📅 Deployment Info

- **Backend URL**: https://portfolio-a4yb.shuttle.app
- **Deployment ID**: depl_01KBR6YRWSMW3JE9CGV8743KT7
- **Status**: ✅ Deployed
- **Changes**: Orders endpoints moved from protected to public routes
