# 🔧 Исправление перехода на Шаг 3

## Проблема
При попытке перейти на **Шаг 3 (Płatność)** возникала блокировка из-за валидации адреса, даже если был выбран **Odbiór osobisty** (самовывоз).

## Причина
Валидация на шаге 2 проверяла наличие адреса **для всех случаев**, включая самовывоз:

```typescript
// ❌ БЫЛО (неправильно)
if (deliveryOptions.method === 'delivery' && !formData.address.trim()) {
  toast.error('Заполните адрес доставки!');
  return; // Блокировало переход
}
```

## Решение

### 1. **Исправлена валидация в `goToNextStep()`**

Теперь адрес проверяется **только для доставки**:

```typescript
// ✅ СТАЛО (правильно)
else if (currentStep === 2) {
  // Validate Step 2: contact info
  if (!formData.name.trim() || !formData.phone.trim()) {
    toast.error('Заполните имя и телефон!');
    return;
  }
  
  // Only validate address if delivery method is 'delivery'
  if (deliveryOptions.method === 'delivery' && !formData.address.trim()) {
    toast.error('Заполните адрес доставки!');
    return;
  }
  
  setCurrentStep(3); // ✅ Переход разрешён для pickup
}
```

### 2. **Добавлена подсказка для пользователей**

При выборе самовывоза показывается hint:

```tsx
{deliveryOptions.method === 'pickup' && (
  <p className="text-[10px] text-center text-muted-foreground mt-2">
    💡 Dla odbioru osobistego adres nie jest wymagany
  </p>
)}
```

### 3. **Кликабельные цифры в Progress Bar**

Теперь можно **напрямую переходить** между шагами:

```tsx
<button onClick={() => setCurrentStep(1)}>1 Dostawa</button>
<button onClick={() => setCurrentStep(2)}>2 Kontakt</button>
<button onClick={() => setCurrentStep(3)}>3 Płatność</button>
```

## Сценарии использования

### ✅ Сценарий 1: Dostawa (доставка)
1. Шаг 1: Выбрать "Dostawa" → Dalej
2. Шаг 2: Заполнить Imię + Telefon + **Adres** → Dalej
3. Шаг 3: Открывается форма оплаты ✅

### ✅ Сценарий 2: Odbiór osobisty (самовывоз)
1. Шаг 1: Выбрать "Odbiór osobisty" → Dalej
2. Шаг 2: Заполнить **только** Imię + Telefon → Dalej
3. Шаг 3: Открывается форма оплаты ✅ (адрес НЕ требуется)

### ✅ Сценарий 3: Прямой переход по цифрам
1. Шаг 1: Нажать на цифру "3" → Сразу переход на Шаг 3 ✅
2. Вернуться: Нажать на цифру "1" → Возврат на Шаг 1 ✅

## Валидация

### Обязательные поля на Шаге 2:

| Поле | Dostawa | Odbiór osobisty |
|------|---------|-----------------|
| Imię | ✅ Обязательно | ✅ Обязательно |
| Telefon | ✅ Обязательно | ✅ Обязательно |
| Email | ⚪ Опционально | ⚪ Опционально |
| Liczba osób | ⚪ По умолчанию 1 | ⚪ По умолчанию 1 |
| Adres | ✅ **Обязательно** | ❌ **НЕ требуется** |
| Mieszkanie, Klatka и т.д. | ⚪ Опционально | ❌ Скрыты |

## Тестирование

### Тест 1: Dostawa с адресом
```bash
# Действия:
1. Выбрать "Dostawa" + "Jak najszybciej"
2. Нажать "Dalej"
3. Заполнить: Imię="Jan", Telefon="+48 123 456 789", Adres="ul. Główna 15"
4. Нажать "Dalej"

# Ожидаемый результат:
✅ Переход на Шаг 3 (Płatność)
```

### Тест 2: Odbiór без адреса
```bash
# Действия:
1. Выбрать "Odbiór osobisty" + "Jak najszybciej"
2. Нажать "Dalej"
3. Заполнить: Imię="Anna", Telefon="+48 987 654 321"
4. НЕ заполнять адрес (поле скрыто)
5. Нажать "Dalej"

# Ожидаемый результат:
✅ Переход на Шаг 3 (Płatność)
💡 Показывается hint: "Dla odbioru osobistego adres nie jest wymagany"
```

### Тест 3: Dostawa без адреса (ошибка)
```bash
# Действия:
1. Выбрать "Dostawa"
2. Нажать "Dalej"
3. Заполнить: Imię="Test", Telefon="+48 111 222 333"
4. НЕ заполнять адрес
5. Нажать "Dalej"

# Ожидаемый результат:
❌ Toast: "Заполните адрес доставки!"
❌ Остаётся на Шаге 2
```

### Тест 4: Клик по цифрам
```bash
# Действия:
1. На Шаге 1 кликнуть цифру "3"

# Ожидаемый результат:
✅ Прямой переход на Шаг 3
✅ Progress bar показывает активный Шаг 3
```

## Файлы изменены

- ✅ `/src/app/checkout/page.tsx` - исправлена валидация + добавлены кликабельные кнопки
- ✅ Добавлены комментарии в код для ясности

## Статус
✅ **ИСПРАВЛЕНО** - переход на Шаг 3 работает корректно для обоих сценариев (Dostawa и Odbiór osobisty)
