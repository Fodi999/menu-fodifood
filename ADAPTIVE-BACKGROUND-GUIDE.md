# 🌈 Адаптивный фон - Инструкция

## 📋 Описание

Система адаптивного фона автоматически меняется в зависимости от:

### 1️⃣ **Времени суток**
- 🌅 **Утро** (5:00 - 12:00): Светлые, тёплые тона с жёлтыми и оранжевыми оттенками
- ☀️ **День** (12:00 - 17:00): Яркие, насыщенные градиенты
- 🌆 **Вечер** (17:00 - 21:00): Мягкие закатные оттенки (оранжевый, розовый, фиолетовый)
- 🌙 **Ночь** (21:00 - 5:00): Тёмные, глубокие градиенты с звёздными эффектами

### 2️⃣ **Погодных условий** (через OpenWeather API)
- ☀️ **Солнечно (clear)**: Яркие, чистые градиенты + светящиеся круги
- ☁️ **Облачно (clouds)**: Приглушённые серо-голубые тона
- 🌧️ **Дождь (rain)**: Холодные синие градиенты + частицы дождя
- ⛈️ **Гроза (thunderstorm)**: Тёмные драматические градиенты + вспышки
- ❄️ **Снег (snow)**: Светлые холодные тона + падающие частицы
- 🌫️ **Туман (fog)**: Мягкие размытые градиенты

### 3️⃣ **Геолокации**
- 🗼 **Tokyo**: Неоновые розово-фиолетовые киберпанк градиенты
- 🗼 **Paris**: Мягкие тёплые тона (бежевый, золотой, персиковый)
- 🏙️ **New York**: Тёмный городской стиль с холодными оттенками
- 🌍 **Default**: Нейтральный tech-gradient (оранжевый primary)

---

## ⚙️ Настройка

### 1. Получить API ключ OpenWeather

1. Зарегистрируйтесь на [OpenWeather](https://openweathermap.org/api)
2. Создайте API ключ (бесплатный план: 1000 запросов/день)
3. Добавьте ключ в `.env.local`:

```bash
NEXT_PUBLIC_OPENWEATHER_API_KEY=your_api_key_here
```

### 2. Включить геолокацию

При первом посещении сайта браузер запросит разрешение на определение местоположения.

**Разрешите доступ** для получения:
- Текущей погоды
- Определения города
- Применения локальной темы

**Если отказать**: будет использован default фон с определением только времени суток.

---

## 🎨 Визуальные эффекты

### Анимированные градиенты
- Плавное движение (scale, x, y)
- Пульсация (opacity)
- Вращение (rotate)

### Светящиеся круги (glowCircles)
Активируются при:
- ☀️ Ясной погоде (днём/ночью)
- ⛈️ Грозе
- 🗼 Теме Tokyo/New York

### Частицы (particles)
Активируются при:
- 🌧️ Дожде
- ❄️ Снеге
- 🌙 Ночи (звёзды)
- 🌅 Утре (пыльца/роса)

---

## 🔧 Кастомизация

### Изменить градиенты для времени суток

Отредактируйте `src/hooks/useAdaptiveBackground.ts`:

```typescript
const backgrounds: Record<TimeOfDay, Record<WeatherCondition, BackgroundConfig>> = {
  morning: {
    clear: {
      gradients: [
        "radial-gradient(...)", // Ваш градиент
      ],
      particles: true,
    },
  },
  // ...
};
```

### Добавить новую локацию

```typescript
const locationThemes: Record<LocationTheme, Partial<BackgroundConfig>> = {
  tokyo: { /* ... */ },
  // Добавьте новый город
  london: {
    gradients: [
      "radial-gradient(ellipse 80% 50% at 50% -20%, oklch(0.70 0.10 240 / 0.3), transparent)",
    ],
  },
};

// Обновите getLocationTheme()
const getLocationTheme = (city: string): LocationTheme => {
  if (cityLower.includes("london")) return "london";
  // ...
};
```

### Настроить анимации

Отредактируйте `src/components/AdaptiveBackground.tsx`:

```typescript
animate={{
  scale: [1, 1.2, 1],     // Изменить масштаб
  opacity: [0.2, 0.4, 0.2], // Изменить прозрачность
}}
transition={{
  duration: 12,            // Скорость анимации (сек)
  repeat: Infinity,        // Бесконечное повторение
}}
```

---

## 🐛 Отладка

### Development mode

В режиме разработки в правом нижнем углу отображается информация:
- Текущее время суток
- Погодные условия
- Температура
- Город и тема

### Проверка в консоли

```javascript
// Откройте DevTools Console
console.log("Time:", timeOfDay);
console.log("Weather:", weather);
console.log("Location:", location);
```

---

## 🚀 Производительность

### Оптимизация
- ✅ Используется `AnimatePresence` для плавных переходов
- ✅ Gradients рендерятся как CSS, не JS
- ✅ Частицы ограничены 30 элементами
- ✅ Debounce на API запросах (кэш на стороне браузера)

### Fallback
Если API недоступен или геолокация отключена:
- Используется только определение времени суток
- Default градиенты без погодных эффектов

---

## 📱 Адаптивность

Фон автоматически адаптируется под:
- 📱 Mobile (упрощённые градиенты)
- 💻 Desktop (полные эффекты)
- 🌗 Light/Dark theme (через Tailwind)

---

## 🎯 Примеры

### Утро + Солнечно + Tokyo
```
🌅 Неоновый розово-жёлтый градиент с частицами света
```

### Ночь + Гроза + New York
```
🌙⛈️ Тёмный драматический градиент с вспышками света
```

### День + Облачно + Paris
```
☀️☁️ Мягкие бежево-голубые тона без дополнительных эффектов
```

---

## 🔗 API Endpoints

### OpenWeather
- **Current Weather**: `https://api.openweathermap.org/data/2.5/weather`
- **Reverse Geocoding**: `https://api.openweathermap.org/geo/1.0/reverse`

### Rate Limits
- Free tier: 1,000 calls/day
- Updates: Каждый час (время суток) + при перезагрузке страницы

---

## 📄 Лицензия

MIT License - используйте свободно! 🎉
