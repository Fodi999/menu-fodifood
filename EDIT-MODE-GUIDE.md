# 📝 Руководство по режиму редактирования

## 🔐 Вход в режим редактирования

1. На главной странице в правом нижнем углу есть панель управления
2. Введите пароль: `admin123`
3. Нажмите кнопку "Разблокировать"

## ✏️ Что можно редактировать

### 🎭 Hero Section (Главная секция)
- **Имя** - кликните на имя
- **Роль** - кликните на "Szef Kuchni / Chef"
- **Описание** - длинный текст с описанием опыта
- **Аватар** - 3 способа загрузки:
  1. 📤 **Upload** - загрузить файл с компьютера
  2. 🔗 **URL** - вставить ссылку на изображение
  3. 🎪 **Drag & Drop** - перетащить файл на фото
- **Email, Телефон, Telegram** - кликните на соответствующие кнопки

**Поддерживаемые форматы изображений:** JPG, PNG, GIF, WebP (до 5MB)

### 🎯 Skills Section (Навыки)
Для каждой карточки навыков:
- **Редактировать навык** - наведите на элемент, нажмите иконку карандаша ✏️
- **Удалить навык** - наведите на элемент, нажмите иконку корзины 🗑️ (минимум 1 навык должен остаться)
- **Добавить навык** - внизу каждой карточки кнопка "+ Dodaj umiejętność"

Клавиши:
- `Enter` - сохранить
- `Escape` - отменить

### 💼 Experience Section (Опыт работы)
Для каждого места работы:
- **Период** - кликните на дату (например, "01.12.2022 — 01.08.2023")
- **Должность** - кликните на "Kucharz" / "Szef Kuchni" и т.д.
- **Компания** - кликните на название ресторана
- **Обязанности** - редактируйте как навыки (✏️ редактировать, 🗑️ удалить, + добавить)
- **Достижения** - редактируйте как навыки

### 🎨 Portfolio Section (Портфолио)
Для каждого проекта:
- **Изображение** - 3 способа загрузки:
  1. 📤 **Upload** - загрузить файл (макс 5MB)
  2. 🔗 **URL** - вставить ссылку
  3. 🎪 **Drag & Drop** - перетащить файл
- **Категория** - кликните на бейдж категории (вверху карточки)
- **Название** - кликните на название при наведении

**Рекомендации:**
- Размер: 800x800px - 1200x1200px
- Формат: JPG для фото блюд
- Оптимизируйте через TinyPNG или Squoosh

**Подробнее:** см. `IMAGE-UPLOAD-GUIDE.md`

### 📞 Contact Section (Контакты)
В режиме редактирования можно изменить:
- **Email** - кликните на email адрес
- **Telefon** - кликните на номер телефона
- **Telegram** - кликните на @username
- **Lokalizacja** - кликните на адрес

В режиме просмотра эти элементы работают как ссылки (mailto:, tel:, telegram).

### 🦶 Footer (Футер)
- Автоматически обновляется при изменении имени в Hero Section

## 🔧 Дополнительные функции

### Кнопка "Сброс" (↻)
- Восстанавливает все данные Dmytro Fomin из defaultResumeData
- Полезно если что-то пошло не так

### Кнопка "Заблокировать" (🔒)
- Выход из режима редактирования
- Возврат в режим просмотра

### Кнопка "Редактировать/Сохранить"
- Переключает режим редактирования
- В режиме редактирования появляется оранжевая полоса вверху страницы

## 💾 Сохранение данных

- **Автоматическое сохранение** - все изменения сохраняются в localStorage браузера
- **Персистентность** - данные сохраняются между перезагрузками страницы
- **Сброс** - используйте кнопку ↻ для возврата к исходным данным

## ⌨️ Горячие клавиши

При редактировании текста:
- `Enter` - сохранить изменения
- `Escape` - отменить изменения

## 🎨 Визуальные подсказки

- **Пунктирная граница** - элемент доступен для редактирования (при наведении)
- **Иконка карандаша** ✏️ - редактировать
- **Иконка галочки** ✓ - сохранить
- **Иконка крестика** ✕ - отменить
- **Иконка корзины** 🗑️ - удалить
- **Иконка плюса** + - добавить новый элемент

## 📱 Мобильная версия

Режим редактирования работает и на мобильных устройствах:
- Тап вместо клика
- Увеличенные кнопки для удобства
- Адаптивные поля ввода

## 🔍 Проверка изменений

1. Внесите изменения в любой секции
2. Выйдите из режима редактирования (кнопка "Сохранить")
3. Перезагрузите страницу
4. Проверьте, что изменения сохранились

## ⚠️ Важно

- **Пароль по умолчанию**: `admin123` (в production рекомендуется изменить в `EditModeToggle.tsx`)
- **localStorage**: данные хранятся локально в браузере
- **Очистка данных**: если очистить localStorage браузера, данные вернутся к defaultResumeData
- **Старые данные**: система автоматически удаляет данные "Иван Иванов" при загрузке

## 🚀 Для разработчиков

### Добавление нового редактируемого поля

```tsx
import { EditableText } from "@/components/EditableText";
import { useResume } from "@/contexts/ResumeContext";

const { resumeData, updateData } = useResume();

<EditableText
  value={resumeData.hero.name}
  onSave={(newValue) => updateData({ hero: { name: newValue } })}
/>
```

### Добавление редактируемого списка

```tsx
import { EditableSkillsList } from "@/components/EditableSkillsList";

<EditableSkillsList 
  items={resumeData.skills.culinary} 
  onUpdate={(newItems) => updateData({ skills: { culinary: newItems } })}
/>
```

### Добавление редактируемого изображения

```tsx
import { EditableImage } from "@/components/EditableImage";

<EditableImage
  src={resumeData.hero.avatar}
  alt="Avatar"
  onSave={(newSrc) => updateData({ hero: { avatar: newSrc } })}
  variant="avatar"
/>
```

## 📄 Структура данных

Все данные хранятся в `src/types/resume.ts`:

```typescript
interface ResumeData {
  hero: { name, role, description, avatar, email, phone, telegram }
  skills: { culinary, soft, certificates, technical }
  experience: [{ period, position, company, responsibilities, achievements }]
  portfolio: [{ title, category, image }]
  contact: { email, phone, telegram, location }
}
```

## 🎯 Советы

1. **Тестируйте в режиме просмотра** - после редактирования всегда проверяйте как выглядит результат
2. **Делайте бэкапы** - копируйте важные данные перед большими изменениями
3. **Используйте сброс** - если запутались, кнопка ↻ вернёт исходные данные
4. **Короткие тексты** - для лучшей читаемости держите тексты лаконичными

## 🐛 Что делать если что-то не работает

1. **Перезагрузите страницу** - часто помогает
2. **Очистите localStorage** - откройте DevTools → Application → Local Storage → удалите resumeData
3. **Нажмите кнопку сброса** - восстановит данные по умолчанию
4. **Проверьте консоль** - откройте DevTools (F12) и посмотрите на ошибки

---

**Удачного редактирования! 🎉**
