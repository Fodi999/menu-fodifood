# 📱 Мобильная адаптация режима редактирования

## ✅ Оптимизация для мобильных устройств

### 🎯 Touch-friendly элементы

#### Размеры кнопок (Apple HIG: минимум 44x44px)
- **Кнопки редактирования**: `36-40px` (9-10 в Tailwind)
- **Иконки**: `14-16px` (3.5-4 в Tailwind)
- **Инпуты**: `36-40px` высота (9-10 в Tailwind)

### 📝 EditableText компонент

#### Адаптивное позиционирование кнопок

**Короткие тексты (≤30 символов)**
```
┌──────────────────┐
│ Текст...    [✓][✗]│
└──────────────────┘
```
- Кнопки справа от инпута
- Инпут с `pr-14 sm:pr-16 md:pr-20`

**Длинные тексты (>30 символов) или multiline**
```
┌─────────────────────┐
│ Очень длинный текст │
│ в несколько строк...│
└─────────────────────┘
                 [✓][✗]
```
- Кнопки под инпутом
- Нет горизонтального overflow

#### Мобильные размеры

```tsx
// Input
className="h-9 sm:h-10 px-2 py-2 sm:px-3 sm:py-2"

// Buttons
className="h-9 w-9 sm:h-10 sm:w-10"

// Icons  
<Check className="w-4 h-4" />
```

#### Видимость кнопок на мобильных

```tsx
// Desktop: показ при hover
// Mobile: всегда видны с opacity 0.7
className="[@media(hover:none)]:opacity-70"
```

### 📋 EditableSkillsList компонент

#### Кнопки управления

**Редактирование элемента**
- Edit (✏️): `h-7 w-7 sm:h-8 sm:w-8`
- Delete (🗑️): `h-7 w-7 sm:h-8 sm:w-8`
- Видны на мобильных: `[@media(hover:none)]:opacity-70`

**Сохранение/Отмена**
- Save (✓): `h-9 w-9 sm:h-10 sm:w-10`
- Cancel (✗): `h-9 w-9 sm:h-10 sm:w-10`

**Кнопка добавления**
- `h-9 sm:h-10` - полная ширина
- `active:scale-[0.98]` - feedback при нажатии

#### Адаптивные отступы

```tsx
gap-0.5 sm:gap-1    // Между кнопками
gap-1.5 sm:gap-2    // Между элементами формы
```

### 🎨 CSS оптимизация

#### Touch-устройства

```css
@media (hover: none) and (pointer: coarse) {
  /* Всегда видимые кнопки */
  .opacity-0.group-hover\:opacity-100 {
    opacity: 0.8 !important;
  }
  
  /* Минимальная область нажатия */
  button, a {
    min-height: 36px;
  }
  
  /* Видимые границы в режиме редактирования */
  .border-dashed.border-transparent {
    border-color: rgba(var(--primary), 0.2) !important;
  }
}
```

#### Touch manipulation

```css
.touch-manipulation {
  touch-action: manipulation;     /* Быстрый клик */
  -webkit-user-select: none;      /* Без выделения текста */
  user-select: none;
}
```

#### Active states (тактильная обратная связь)

```tsx
active:scale-95      // Маленькие кнопки
active:scale-[0.98]  // Большие кнопки/элементы
```

### 📦 Контейнеры и overflow

#### Card компоненты

```tsx
<Card className="overflow-hidden">
  <CardContent className="overflow-x-hidden">
    {/* Контент */}
  </CardContent>
</Card>
```

#### Flex контейнеры

```tsx
<div className="flex gap-1.5 sm:gap-2 min-w-0 max-w-full">
  <Input className="flex-1 min-w-0" />
  <Button className="flex-shrink-0" />
</div>
```

### 📏 Брейкпоинты Tailwind

| Брейкпоинт | Размер | Использование |
|------------|--------|---------------|
| `(default)` | < 640px | Мобильные |
| `sm:` | ≥ 640px | Планшеты портрет |
| `md:` | ≥ 768px | Планшеты ландшафт |
| `lg:` | ≥ 1024px | Десктоп |

### 🎯 Рекомендации по дизайну

#### ✅ Правильно

```tsx
// Touch-friendly размеры
<button className="h-10 w-10 touch-manipulation active:scale-95">
  <Icon className="w-4 h-4" />
</button>

// Видимость на мобильных
<div className="opacity-0 group-hover:opacity-100 [@media(hover:none)]:opacity-70">

// Адаптивные отступы
<div className="px-2 py-2 sm:px-3 sm:py-2">
```

#### ❌ Неправильно

```tsx
// Слишком маленькие кнопки
<button className="h-6 w-6">  

// Скрытые элементы на мобильных
<div className="opacity-0 group-hover:opacity-100">

// Фиксированные отступы
<div className="px-3 py-2">
```

### 🔧 Тестирование

#### Проверка на мобильных

1. **Chrome DevTools**
   - Toggle device toolbar (Cmd+Shift+M)
   - Выбрать iPhone/Android устройство
   - Включить "Show media queries"

2. **Что проверять**
   - ✅ Кнопки ≥ 36px (удобно нажимать)
   - ✅ Видимость контролов без hover
   - ✅ Нет горизонтального скролла
   - ✅ Инпуты помещаются в карточки
   - ✅ Feedback при нажатии (scale animation)
   - ✅ Текст не обрезается

3. **Реальные устройства**
   - iOS Safari
   - Android Chrome
   - Портретная/ландшафтная ориентация

### 📱 Особенности платформ

#### iOS Safari
- `-webkit-tap-highlight-color: transparent` - убирает серое выделение
- `touch-action: manipulation` - отключает двойной тап для зума
- `user-select: none` на кнопках - нет выделения текста

#### Android Chrome
- Поддержка `active:` псевдокласса
- `:focus-visible` для клавиатурной навигации
- Нативный zoom при фокусе на input (отключен через viewport)

### 🎨 Анимации для touch

```tsx
// При нажатии - уменьшение
active:scale-95

// Плавный переход
transition-all

// Пример
className="touch-manipulation active:scale-95 transition-all"
```

### 📊 Производительность

#### Оптимизация для мобильных

1. **CSS containment**
   - `overflow-hidden` на карточках
   - `will-change` для анимируемых элементов

2. **Debounce для onChange**
   - Не используем - реактивное обновление

3. **Lazy rendering**
   - AnimatePresence для плавного появления/исчезновения

## 🚀 Итого

### Все элементы редактирования адаптированы:

- ✅ **EditableText**: умное позиционирование кнопок (справа/внизу)
- ✅ **EditableSkillsList**: touch-friendly кнопки и инпуты
- ✅ **EditableImage**: большие области для drag-drop
- ✅ **Все карточки**: `overflow-hidden` для предотвращения выхода
- ✅ **Кнопки**: минимум 36px, всегда видны на мобильных
- ✅ **Инпуты**: адаптивные размеры h-9/h-10
- ✅ **Анимации**: `active:scale` для feedback
- ✅ **CSS**: touch-оптимизация через media queries

**Результат**: Полностью функциональный режим редактирования на мобильных устройствах! 🎉
