# 🧪 Тестирование WebSocket - Пошаговая инструкция

## Статус развертывания

- ✅ Backend скомпилирован без ошибок (только warnings)
- ⏳ Деплой на Shuttle в процессе: `depl_01KBR1CAN332V09A9GGHHZWKRB`
- 🎯 Production URL: `https://portfolio-a4yb.shuttle.app`
- 🔌 WebSocket URL: `wss://portfolio-a4yb.shuttle.app/api/ws`

## Исправленные проблемы

1. ✅ **SQLx compile-time check** - заменены `query!` на `query_scalar`
2. ✅ **Type mismatch** - использован `BigDecimal` вместо `rust_decimal`
3. ✅ **Axum 0.8 compatibility** - `Message::Text(json.into())`
4. ✅ **Deployment directory** - явный `cd` в `/backend`

## Шаг 1: Проверка деплоя

После завершения деплоя проверить:

```bash
# Проверить статус
cargo shuttle deployment list

# Или проверить health endpoint
curl https://portfolio-a4yb.shuttle.app/health
```

**Ожидаемый результат:**
```json
{"status": "healthy"}
```

## Шаг 2: Тест WebSocket подключения (Browser Console)

Открыть DevTools в браузере и выполнить:

```javascript
// Подключиться к WebSocket
const ws = new WebSocket('wss://portfolio-a4yb.shuttle.app/api/ws');

// События
ws.onopen = () => console.log('✅ WebSocket подключен!');
ws.onmessage = (event) => {
  const message = JSON.parse(event.data);
  console.log('📨 Получено:', message);
};
ws.onerror = (error) => console.error('❌ Ошибка:', error);
ws.onclose = () => console.log('🔌 WebSocket закрыт');

// Отправить ping
ws.send(JSON.stringify({ type: 'ping' }));
```

**Ожидаемый результат:**
1. Сообщение: `✅ WebSocket подключен!`
2. Через ~100ms: `📨 Получено:` с аналитикой (analytics_update)
3. Через 30 сек: ping сообщения

## Шаг 3: Тест на дашборде

1. **Войти как админ:**
   - URL: `https://твой-фронтенд.vercel.app/admin/login`
   - Login: `admin`
   - Password: `admin123`

2. **Открыть дашборд:**
   - URL: `/admin/dashboard`
   - Проверить индикатор в header

3. **Проверить статус:**
   ```
   ✅ Должно быть: 🟢 Онлайн (Реальное время)
   ❌ Если: ⚪ Офлайн - проблема с подключением
   ```

4. **Открыть DevTools → Network → WS:**
   - Должно быть активное WebSocket соединение
   - Frames: ping/pong каждые 30 секунд
   - Messages: analytics_update при подключении

## Шаг 4: Тест уведомлений (после реализации orders API)

1. Открыть 2 вкладки:
   - **Вкладка 1:** Дашборд (оставить открытым)
   - **Вкладка 2:** Сайт (главная страница)

2. На сайте оформить заказ:
   - Добавить товары в корзину
   - Перейти в чекаут
   - Заполнить форму
   - Нажать "Оформить заказ"

3. На дашборде должно появиться:
   - 🛒 Toast уведомление: "Новый заказ #ORD-..."
   - Обновленная статистика (заказы +1, доход +X)

## Ожидаемые сообщения

### 1. При подключении (analytics_update)
```json
{
  "type": "analytics_update",
  "total_orders": 0,
  "total_revenue": "0",
  "pending_orders": 0
}
```

### 2. Ping (каждые 30 сек)
```json
{
  "type": "ping"
}
```

### 3. Pong (ответ на ping)
```json
{
  "type": "pong"
}
```

### 4. Новый заказ (будущее)
```json
{
  "type": "new_order",
  "order_id": 123,
  "order_number": "ORD-20241205-001",
  "customer_name": "Иван Иванов",
  "total": "150.50"
}
```

## Команды для проверки в терминале

### Проверить логи деплоя:
```bash
cd backend
cargo shuttle logs
```

### Проверить список деплоев:
```bash
cargo shuttle deployment list
```

### Тест с curl (не работает для WS, только HTTP):
```bash
# Health check
curl https://portfolio-a4yb.shuttle.app/health

# Categories
curl https://portfolio-a4yb.shuttle.app/api/categories
```

### Тест WebSocket через wscat (если установлен):
```bash
# Установить wscat
npm install -g wscat

# Подключиться
wscat -c wss://portfolio-a4yb.shuttle.app/api/ws

# Отправить ping
> {"type":"ping"}

# Должен прийти pong
< {"type":"pong"}
```

## Troubleshooting

### Проблема: WebSocket не подключается

**Проверить:**
1. Backend запущен: `curl https://portfolio-a4yb.shuttle.app/health`
2. URL правильный: `wss://` (НЕ `ws://`)
3. CORS настроен правильно
4. Firewall не блокирует WebSocket

**Решение:**
- Проверить логи: `cargo shuttle logs`
- Проверить браузер Console на ошибки
- Попробовать reconnect: `wsService.disconnect(); wsService.connect()`

### Проблема: Индикатор показывает "Офлайн"

**Причины:**
1. Backend не отвечает
2. WebSocket endpoint недоступен
3. Проблема с CORS
4. Таймаут соединения

**Решение:**
1. Открыть DevTools → Console → искать ошибки WebSocket
2. Проверить Network → WS → статус соединения
3. Проверить backend health: `curl https://portfolio-a4yb.shuttle.app/health`

### Проблема: Соединение часто разрывается

**Причина:** 
- Таймаут на прокси/load balancer
- Firewall

**Решение:**
- Heartbeat уже реализован (ping каждые 30 сек)
- Автоматический reconnect работает (до 5 попыток)

### Проблема: Нет уведомлений о заказах

**Причина:**
- Backend еще не отправляет события при создании заказов
- Это нормально - нужно интегрировать broadcast в orders handler

**Решение:**
- Добавить в `handlers/orders.rs`:
  ```rust
  // После создания заказа
  ws_state.broadcast(WsMessage::NewOrder { ... });
  ws_state.send_analytics_update().await;
  ```

## Метрики успеха

✅ **Все работает если:**
1. Индикатор показывает 🟢 Онлайн
2. В DevTools → WS видно активное соединение
3. Приходят ping/pong сообщения
4. Статистика загружается при подключении
5. Нет ошибок в Console

## Следующие шаги

После успешного деплоя:

1. **Phase 1: ✅ Базовая инфраструктура** (завершено)
   - WebSocket сервер
   - WebSocket клиент
   - Подключение к дашборду
   - Индикатор статуса

2. **Phase 2: 🚧 Интеграция с заказами** (следующее)
   - Broadcast при создании заказа
   - Broadcast при смене статуса
   - Toast уведомления на дашборде

3. **Phase 3: 📋 Улучшения** (будущее)
   - JWT аутентификация для WS
   - Rate limiting
   - Приватные каналы
   - Metrics и monitoring

## Полезные ссылки

- **Production API:** https://portfolio-a4yb.shuttle.app
- **WebSocket endpoint:** wss://portfolio-a4yb.shuttle.app/api/ws
- **Документация:** [WEBSOCKET-INTEGRATION.md](./WEBSOCKET-INTEGRATION.md)
- **Shuttle docs:** https://docs.shuttle.rs
- **Axum WebSocket:** https://docs.rs/axum/latest/axum/extract/ws/index.html

---

**Автор:** GitHub Copilot  
**Дата:** 5 декабря 2024  
**Версия:** 1.0
