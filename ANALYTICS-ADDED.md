# ✅ Аналитика посещаемости и активности - Добавлено

## Что добавлено

### 1. Система аналитики (Analytics Service)

**Файл**: `src/lib/analytics.ts`

Сервис для отслеживания пользовательской активности с хранением в `localStorage`.

#### Отслеживаемые метрики:

1. **totalVisits** - Всего посещений сайта
2. **totalOrders** - Оформлено заказов
3. **totalCartItems** - Текущее количество товаров в корзине
4. **totalMenuViews** - Просмотры страницы меню
5. **lastVisit** - Дата последнего посещения
6. **firstVisit** - Дата первого посещения

#### API методы:

```typescript
analytics.trackVisit()              // Отследить посещение
analytics.trackOrder()              // Отследить заказ
analytics.trackMenuView()           // Отследить просмотр меню
analytics.updateCartItems(count)    // Обновить количество в корзине
analytics.getData()                 // Получить все данные
analytics.reset()                   // Сбросить аналитику
```

### 2. Интеграция в компоненты

#### Главная страница (`/`)
```tsx
<AnalyticsTracker /> // Трекает посещения
```

#### Страница меню (`/menu`)
```tsx
useEffect(() => {
  analytics.trackMenuView();
}, []);
```

#### Корзина (`CartContext`)
```tsx
useEffect(() => {
  const totalCount = items.reduce((sum, item) => sum + item.quantity, 0);
  analytics.updateCartItems(totalCount);
}, [items]);
```

#### Оформление заказа (`/checkout`)
```tsx
analytics.trackOrder(); // После успешного создания заказа
```

### 3. Дашборд администратора

#### Новая секция "Аналитика посещаемости"

**4 новые карточки статистики:**

1. **Посещения сайта** 👁️
   - Счетчик всех посещений
   - Иконка: Eye
   - Описание: "Всего посещений"

2. **Заказы (локально)** 🛒
   - Количество оформленных заказов
   - Иконка: ShoppingCart
   - Описание: "Оформлено заказов"

3. **Товаров в корзине** 📦
   - Текущее количество товаров
   - Иконка: Package
   - Описание: "Текущее количество"

4. **Просмотры меню** 🍜
   - Количество переходов на /menu
   - Иконка: UtensilsCrossed
   - Описание: "Переходов на меню"

#### Карточка "Детали аналитики"

Показывает:
- Первое посещение (дата и время)
- Последнее посещение (дата и время)
- **Конверсия в заказ** - процент посетителей, оформивших заказ

```
Конверсия = (Заказы / Посещения) × 100%
```

## Технические детали

### Хранение данных

**localStorage key**: `fodifood_analytics`

**Структура данных**:
```typescript
{
  totalVisits: 0,
  totalOrders: 0,
  totalCartItems: 0,
  totalMenuViews: 0,
  lastVisit: "2025-12-05T20:30:00.000Z",
  firstVisit: "2025-12-05T18:00:00.000Z"
}
```

### Автоматическое обновление

1. **Посещения**: Автоматически при загрузке главной страницы
2. **Заказы**: Автоматически после успешного оформления
3. **Корзина**: Автоматически при добавлении/удалении товаров
4. **Меню**: Автоматически при заходе на страницу /menu

### Компонент AnalyticsTracker

```tsx
'use client';

import { useEffect } from 'react';
import { analytics } from '@/lib/analytics';

export function AnalyticsTracker() {
  useEffect(() => {
    analytics.trackVisit();
  }, []);

  return null; // Не рендерит ничего
}
```

## Визуальное оформление

### Секция аналитики на дашборде

```
┌─────────────────────────────────────────────────────┐
│ 📈 Аналитика посещаемости                           │
├─────────────────────────────────────────────────────┤
│ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐   │
│ │ 👁️ 42   │ │ 🛒 5    │ │ 📦 3    │ │ 🍜 15   │   │
│ │ Посещ.  │ │ Заказы  │ │ Корзина │ │ Меню    │   │
│ └─────────┘ └─────────┘ └─────────┘ └─────────┘   │
│                                                     │
│ ┌─────────────────────────────────────────────┐   │
│ │ Детали аналитики                            │   │
│ │ Первое посещение: 05.12.2025, 18:00        │   │
│ │ Последнее посещение: 05.12.2025, 20:30     │   │
│ │ Конверсия в заказ: 11.9%                    │   │
│ └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

## Примеры использования

### Просмотр аналитики в консоли

```javascript
// В консоли браузера
import { analytics } from '@/lib/analytics';

// Получить все данные
const data = analytics.getData();
console.log(data);

// Сбросить аналитику
analytics.reset();
```

### Ручное отслеживание

```typescript
// Отследить событие вручную
analytics.trackVisit();
analytics.trackOrder();
analytics.trackMenuView();
analytics.updateCartItems(5);
```

## Расчет метрик

### Конверсия в заказ

```typescript
const conversionRate = analyticsData.totalVisits > 0
  ? ((analyticsData.totalOrders / analyticsData.totalVisits) * 100).toFixed(1)
  : 0;
```

**Пример**:
- Посещений: 42
- Заказов: 5
- Конверсия: (5 / 42) × 100 = 11.9%

### Среднее количество товаров

```typescript
const averageCartSize = analyticsData.totalOrders > 0
  ? (analyticsData.totalCartItems / analyticsData.totalOrders).toFixed(1)
  : 0;
```

## Преимущества

### ✅ Для администратора:
1. **Мгновенная аналитика** - данные доступны сразу
2. **Без бэкенда** - работает полностью на клиенте
3. **Конверсия** - понимание эффективности сайта
4. **Активность** - видно, сколько людей в корзине

### ✅ Для бизнеса:
1. **Понимание трафика** - сколько людей посещают
2. **Оценка интереса** - просмотры меню
3. **Анализ конверсии** - процент завершенных заказов
4. **Данные для оптимизации** - где терять клиентов

## Ограничения

### ⚠️ localStorage:
- Данные хранятся только в браузере
- Привязаны к конкретному устройству
- Очищаются при очистке браузера
- Не синхронизируются между устройствами

### 🔮 Будущие улучшения:

1. **Синхронизация с бэкендом**
   - Отправка данных на сервер
   - Агрегация по всем пользователям
   - Централизованная аналитика

2. **Расширенные метрики**
   - Время на сайте
   - Средний чек
   - Популярные товары
   - Брошенные корзины

3. **Визуализация**
   - Графики посещаемости
   - Диаграммы конверсии
   - Тренды по дням/неделям

4. **Аналитика в реальном времени**
   - WebSocket подключение
   - Live обновления дашборда
   - Уведомления о новых заказах

## Тестирование

### Сценарий 1: Первое посещение
1. ✅ Открыть главную страницу
2. ✅ Проверить `totalVisits = 1`
3. ✅ Проверить установлены `firstVisit` и `lastVisit`

### Сценарий 2: Добавление в корзину
1. ✅ Добавить 3 товара в корзину
2. ✅ Проверить `totalCartItems = 3`
3. ✅ На дашборде отображается "3"

### Сценарий 3: Оформление заказа
1. ✅ Оформить заказ
2. ✅ Проверить `totalOrders = 1`
3. ✅ Проверить `totalCartItems = 0` (корзина очищена)
4. ✅ На дашборде конверсия обновилась

### Сценарий 4: Просмотр меню
1. ✅ Перейти на /menu
2. ✅ Проверить `totalMenuViews увеличилось`
3. ✅ На дашборде отображается правильное число

### Сценарий 5: Сброс данных
1. ✅ В консоли: `analytics.reset()`
2. ✅ Все счетчики обнулены
3. ✅ Даты установлены в текущее время

## Структура файлов

```
src/
├── lib/
│   └── analytics.ts                [СОЗДАНО]
├── components/
│   └── AnalyticsTracker.tsx        [СОЗДАНО]
├── contexts/
│   └── CartContext.tsx             [ОБНОВЛЕНО]
├── app/
│   ├── page.tsx                    [ОБНОВЛЕНО]
│   ├── menu/
│   │   └── page.tsx                [ОБНОВЛЕНО]
│   ├── checkout/
│   │   └── page.tsx                [ОБНОВЛЕНО]
│   └── admin/
│       └── dashboard/
│           └── page.tsx            [ОБНОВЛЕНО]
```

## Пример данных на дашборде

После активного использования сайта:

```
Статистика бэкенда:
├── Всего заказов: 0 (Скоро доступно)
├── Общий доход: 0.00 zł (Скоро доступно)
├── Категории: 5
└── Блюда в меню: 15

Аналитика посещаемости:
├── Посещения сайта: 127
├── Заказы (локально): 18
├── Товаров в корзине: 0
├── Просмотры меню: 89
└── Конверсия в заказ: 14.2%
```

## Заключение

Теперь у вас есть полноценная система аналитики, которая отслеживает:
- 👁️ **Посещения** - кто приходит на сайт
- 🛒 **Заказы** - сколько завершенных заказов
- 📦 **Корзина** - что сейчас в корзинах
- 🍜 **Меню** - интерес к меню
- 📊 **Конверсия** - эффективность сайта

Все данные доступны сразу на дашборде администратора! 🎉
