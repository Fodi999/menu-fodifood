# 📝 ПРИМЕР КОНФИГУРАЦИИ ПЕРЕМЕННЫХ ОКРУЖЕНИЯ

# ==========================================
# 🔧 ДЛЯ ЛОКАЛЬНОЙ РАЗРАБОТКИ
# ==========================================
# Скопируйте этот файл как .env.local для разработки:
# cp .env.example .env.local

# Database - локальный PostgreSQL
DATABASE_URL="postgresql://user:password@localhost:5432/fodisushi"

# Или используйте Docker:
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/fodisushi"

# NextAuth - локальный сервер
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your_local_secret_here"

# ⚠️ Как сгенерировать NEXTAUTH_SECRET:
# openssl rand -base64 32


# ==========================================
# � ДЛЯ ПРОДАКШЕНА (VERCEL + NEON)
# ==========================================
# НЕ создавайте файл .env.production!
# Добавьте эти переменные в Vercel → Settings → Environment Variables

# Database - Neon PostgreSQL (с Pooling!)
# DATABASE_URL="postgresql://neondb_owner:PASSWORD@ep-xxx-pooler.REGION.aws.neon.tech/neondb?connect_timeout=15&sslmode=require"

# NextAuth - ваш домен на Vercel
# NEXTAUTH_URL="https://menu-fodifood.vercel.app"
# NEXTAUTH_SECRET="тот_же_секрет_что_и_локально_или_новый"

# 📌 ВАЖНО:
# 1. В Neon используйте "Pooled connection" (с -pooler в URL)
# 2. NEXTAUTH_SECRET должен быть одинаковым везде (или разным для разных окружений)
# 3. Файл .env.local не попадает в Git (.gitignore)

