# 🔧 FRONTEND CONFIGURATION EXAMPLE

# API Backend URL (Elixir Gateway)
# Development: http://localhost:4000/api
# Production: https://your-elixir-gateway.com/api
NEXT_PUBLIC_API_URL="http://localhost:4000/api"

# WebSocket URL (Elixir Gateway)
# Development: ws://localhost:4000/socket/websocket
# Production: wss://your-elixir-gateway.com/socket/websocket
# Rust Gateway API
NEXT_PUBLIC_RUST_API="http://127.0.0.1:8000/api/v1"

# WebSocket для UI events и инсайтов
NEXT_PUBLIC_INSIGHT_WS="ws://127.0.0.1:8000/api/v1/insight"

# WebSocket для уведомлений о заказах
NEXT_PUBLIC_WS_URL="ws://127.0.0.1:8000/api/v1/admin/ws"

# User Roles
NEXT_PUBLIC_USER_ROLE="user"
NEXT_PUBLIC_ADMIN_ROLE="admin"

# JWT Secret (must match Go and Elixir backends)
JWT_SECRET="your-secret-key-here"

# NextAuth Configuration (если используется)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

# 💡 Инструкции:
# 1. Скопируйте этот файл в .env.local
# 2. Замените значения на реальные
# 3. Все API запросы идут через Elixir Gateway (port 4000)
# 4. Elixir Gateway проксирует запросы в Go Backend (port 8080)
# 5. JWT_SECRET должен совпадать во всех сервисах
